ReplayPro v24.5 MouseEvent Fix

Fixed:
- resolved ambiguous MouseEventArgs / MouseButtonEventArgs errors
- timeline drag handlers now explicitly use WPF event arg types

Included:
- same timeline drag build as v24.4
- same shuttle slider return-to-center behavior
- same loop controls

This is a compile-fix build focused on the Visual Studio error shown in the screenshot.
